# Occipital-RS — Build Roadmap

Phased, each phase has a **gate** (the thing that must work to move on). Phases 0–8 are the
standalone Occipital-RS; Phase 9 is the ApexOS-RS integration. Mirror Cerebro's discipline:
clippy-clean, tests per crate, build incrementally.

| Phase | Feature | Gate | Status |
|-------|---------|------|--------|
| 0 | **Scaffold** — Cargo workspace (`occipital` + `-mcp`/`-api`/`-cli`), config module, tier detection, CI clippy-clean | `cargo build --workspace` green; `occipital-mcp` does the MCP handshake (initialize/tools-list) | ✓ — workspace builds; `occipital-mcp` handshake + 5-tool `tools/list` live (honest not-implemented `tools/call`); `Config::from_env` + Nano/Micro tier + compile-time polite-defaults guard; CLI `status`; `-api` `/health`; 9 tests, clippy `-D warnings` clean; CI workflow added |
| 1 | **Polite fetcher** — `Fetcher` trait, `reqwest` client, per-domain token bucket + jitter + concurrency cap + backoff, robots cache, honest UA | fetch a URL; an integration test proves N requests to one domain are spaced ≥ the interval; robots `Disallow` honored | ✓ — `Fetcher` trait + `PoliteFetcher` (rustls reqwest, honest UA, redirect cap, body cap); per-domain `DomainLimiter` with **additive-only** jitter (floor never violated), global concurrency `Semaphore`, polite backoff honoring `Retry-After`; cached robots parser (UA groups, longest-match Allow-override, `*`/`$`). 19 unit tests (spacing + robots gate pure-tested) + a live `example.com` fetch; clippy `-D warnings` clean |
| 2 | **Reader-mode** — HTML → `Page{title,markdown,links,content_hash}` | a real page returns clean markdown + a sane link list, no chrome; emoji/CJK safe | ✓ — single-pass DOM→Markdown via `scraper`: `select_main` (largest `<main>`/`<article>`/`[role=main]`, body fallback), boilerplate skip-list, common-element conversion (headings/bold/italic/lists/code/pre/blockquote/img), links resolved to absolute + deduped in the same pass, FNV-1a `content_hash`, depth-guarded. UTF-8 throughout (emoji/CJK safe). 9 fixture tests + a live `example.com` round-trip; clippy `-D warnings` clean |
| 3 | **Search providers** — `SearchProvider` trait + `duckduckgo` (HTML) + `searxng` (JSON); `web_search`/`web_fetch` MCP tools | `web_search("…")` returns ranked results live; `web_fetch` returns reader-mode | ✓ — `SearchProvider` trait; `duckduckgo` (HTML scrape, uddg-redirect decode, fail-soft) + `searxng` (JSON), `provider_for` selection w/ fallback; `Fetcher::get_unchecked` (robots-bypass for deliberate provider API calls); `Engine` owns fetcher+provider; `web_search`/`web_fetch` wired in `occipital-mcp` returning flat `kind`-discriminated results. Verified LIVE through the MCP binary (DDG `rust` query → 3 ranked hits; `web_fetch` → reader-mode); 44 tests, clippy `-D warnings` clean |
| 4 | **Cache (read-through)** — SQLite `pages`, FTS5, first-hit lookup, conditional GET, write-back | second fetch served from cache; stale entry refreshes via `304`; re-asked search makes zero live requests | ✓ — SQLite `pages` + `searches` store (`Cache`); `Fetcher::get_conditional` (If-None-Match/If-Modified-Since, captures ETag/Last-Modified, 304 passthrough); `Engine` read-through for `fetch` + `search` with `fresh` bypass, TTL freshness policy, pin-preserving upsert; `web_save`/`web_forget` now live. Verified LIVE through the binary (2nd fetch `from_cache`, normalized re-asked search `from_cache`, save+forget); 50 tests incl. deterministic 304-refresh; clippy `-D warnings` clean. (FTS5 column deferred to Phase 5 with recall) |
| 5 | **Embeddings + semantic recall** — bge-small (gated), `web_recall` over read pages only | semantic query over cached pages (Micro+); FTS5 fallback works with `OCCIPITAL_EMBED_MODEL=""` (Nano) | ✓ — `Embedder` trait + feature-gated `FastEmbedder` (fastembed/bge-small) + cosine; `fastembed` is an **optional `embeddings` feature** (Nano default build excludes the ONNX runtime). Cache gains FTS5 `pages_fts` + an `embeddings` table (f32 BLOBs), kept in sync on put/delete. `Engine::index` embeds on fetch; `web_recall` does cosine semantic recall (Micro+) or FTS5 keyword (Nano). Verified LIVE both tiers: real ONNX recall ranked example.com 0.732 for its topic; Nano FTS5 keyword hit. 56 tests, clippy `-D warnings` clean. (brute-force cosine at first; the sqlite-vec ANN index — row 18 — now leads, this path is the fallback) |
| 6 | **Decay + ranking + GC** — salience decay, `relevance×freshness×salience` ranking, prune stale unread pages | an old unread page sinks in ranking and is GC'd; a `web_save`-pinned page survives | ✓ — pure `decay` module (disuse half-life, `effective_salience`); cache reinforces salience on access (ACT-R) + `all_page_meta`; `recall` scales relevance by disuse-decay (stale sinks); `Engine::gc` prunes unpinned pages below the effective-salience floor past min-age; CLI `occipital gc`; config knobs (`OCCIPITAL_DECAY_HALFLIFE_SECS`/`GC_MIN_SALIENCE`/`GC_MIN_AGE_SECS`). Verified LIVE (gc pruned the unpinned page, pinned survived) + decay-ranking unit-tested. 63 tests, clippy `-D warnings` clean |
| 7 | **Keyed providers** — `brave`/`tavily`/`bing` + key CRUD (0600), provider selection | keyed search works when a key is set; falls back to scrape gracefully when not | ✓ — generalized `Fetcher::request` (custom headers / POST via `HttpRequest`); Brave (GET + `x-subscription-token`), Tavily (POST JSON), Bing (GET + `ocp-apim-subscription-key`) providers, all fail-soft; `keys` store (env `OCCIPITAL_<P>_KEY` overlay on a 0600 JSON file, redacted list); `provider_for` selects keyed only when a key is present, else DDG fallback; CLI `occipital keys set/list/rm`. Verified LIVE (CRUD + redaction + 0600 + graceful fallback to DDG); keyed request-building + parsing unit-tested via a recording mock. 71 tests, clippy `-D warnings` clean |
| 8 | **API + CLI surfaces** — axum REST + clap CLI parity (query, page CRUD, provider keys, gc, stats) | full management + query surface; CLI `occipital search/fetch/recall/gc/keys` | ✓ — CLI now async with live `search`/`fetch`/`recall` (+ `gc`/`keys`/`status`); `occipital-api` REST: `/search` `/fetch` `/recall` `/save` `/forget` `/gc` `/stats` `/keys` (+ `/keys/:provider` PUT/DELETE), `ApiError`→500 JSON; `Cache::stats`/`Engine::stats`+`semantic`. Verified LIVE: CLI returned 5 DDG hits + reader-mode + cached recall; API curl across the whole route set (search/fetch/keys-redacted/stats/gc). 71 tests, clippy `-D warnings` clean |
| 9 | **ApexOS-RS integration** — `register_mcp_server` wire-up; `view`-payload follow-along window in `ui-slint`; "go here" click → `user_prompt` nudge | APEX searches the live web politely; a human follows along on the desktop and steers with a click | ✓ — shipped **in ApexOS-RS** (this row was stale): install.sh auto-provisions occipital-mcp (clone/build/register, tier-split FTS5 vs `--features embeddings`); ui-slint renders the `kind`-discriminated payloads (📖 reader window, `occipital_payload()` recovery, LIVE/CACHED badge, trail) and the click/URL-bar steer sends the queued `user_prompt` nudge through the TurnGate. See ApexOS-RS `docs/occipital.md` |
| 10 | **Knowledge hub: distillation (LLM curation)** — `curate` module (tiered `Distiller`: Ollama → Anthropic, mirroring Cerebro's `describe_image`), `distillations` store + `distill_fts`, `web_distill` tool + CLI `distill` + API `POST /distill`; `web_recall` serves distilled summary + tags | a cached page distills into summary/key-points/entities/tags; recall returns the distillation (not a raw snippet) and finds pages by distilled terms on Nano; unchanged content re-asks free | ✓ — `OCCIPITAL_CURATE_BACKEND` = `auto`\|`ollama`\|`anthropic`\|`off` (auto = local-first, API fallback); explicit-only (tool/CLI/API — nothing spends tokens on its own); sweep is bounded (default 3, ≤10) + per-page fail-soft; hash-mismatch marks stale for re-distill. 88 tests (parse/clean/prompt pure-tested; engine via a `MockDistiller`), clippy `-D warnings` clean both tiers; the no-backend and failure paths verified live through the CLI. **Live LLM round-trip still pending** (dev box had no Ollama/key) — one `occipital distill <url>` with a backend at hand closes it; the transport mirrors cerebro `vision.rs` verbatim |

| 11 | **Knowledge hub: auto-curation** — background distill sweep in the resident servers (`occipital-mcp`/`-api`), budget-guarded | with `OCCIPITAL_AUTO_DISTILL=local\|on`, newly-read pages distill themselves within an interval, the rolling-24h cap pauses the sweep, and `off` (default) changes nothing | ✓ — `AutoDistill` mode (`off` default \| `local` = Ollama-pinned, never the API \| `on` = the configured backend); `Engine::auto_distill_tick` (shared `distill_targets` loop with the explicit verb): batch = sweep-default clamped to the remaining budget (`Cache::distilled_since` counts ALL distillations — a total-spend guard, restart-proof); quiet when off/capped/nothing-pending. Interval task in both resident servers (`OCCIPITAL_AUTO_DISTILL_INTERVAL_SECS`, default 300, floor 30); CLI `status` + API `/stats` surface curation/auto. Tick/budget/matrix unit-tested (mock distiller) |

| 17 | **Knowledge hub: relate + surface** — `relate` module (pure overlap scoring: entities ×2, tags ×1, case-insensitive, self/zero-overlap excluded, deterministic ties), `Cache::all_distill_meta` (title joined from pages), `Engine::related` + `web_related` tool + CLI `related` + API `GET /related`; every fresh distillation carries its top-3 neighbours inline (`DistilledPage::related`, off the wire when empty) | a curated page lists its neighbours with the shared terms as the edge label; an undistilled page gets an honest "distill it first" (cached vs not-cached worded apart); an empty result carries `distilled_total` so "unconnected" and "nothing curated yet" read differently | ✓ — computed live from `distillations`, never stored (a link table would stale on re-distill/forget); relate does **zero network + zero LLM** (no politeness surface, no live gate needed); embeddings deliberately not consulted (shared entities are the knowledge-graph signal, cosine says "the prose is alike"). 168 tests workspace-wide (relate pure-tested; engine + dispatch via mock distillers), clippy `-D warnings` clean |

| 18 | **Semantic recall at scale: sqlite-vec ANN** — vec0 index (`page_vectors`, keyed by the stable pages rowid) behind the `embeddings` feature; `Cache::vec_search` KNN leads recall, brute-force cosine over the `embeddings` BLOBs is the automatic fallback | recall answers from the index when it can (over-fetch 5× so decay re-ranking keeps headroom); feature-off / extension-absent / pre-index stores behave exactly as before; a store predating the index heals itself | ✓ — extension registered per-process via `sqlite3_auto_extension` (the CerebroCortex pattern); the index table is created **lazily at the first stored vector, sized to that vector's dimension** (no dim config — any embedder fits, tests run a 3-dim index); the `embeddings` BLOB table stays the source of truth and `vec_upsert` **self-heals** the index from it in one statement (pre-ANN stores backfill on their next write); any vec0 failure parks the index (warn once) and recall brute-forces — never an error upward; `delete_page` clears the index row while the rowid still exists (rowids get reused). Tests run the SAME recall suite through both paths (feature off = brute, on = vec0) + 3 dedicated index tests (ANN/brute parity, refresh/delete truth, backfill). 153 lib tests, clippy clean both tiers |

Knowledge-hub follow-ons (in "living knowledge hub" order, ApexOS BACKLOG #10):
semantic dedup on ingest, digests ("what did I read about X"), freshness/re-distill.
(✓ done since this list was written: the ui-slint distill card — ApexOS-RS #287;
`ingest_file` — landed in cerebro-mcp, ApexOS-RS #279; relate/surface — row 17.)

### Agent-browsing expansion (Phases 12–16) — design locked in [agent-browsing.md](agent-browsing.md)

| Phase | Feature | Gate | Status |
|-------|---------|------|--------|
| 12 | **Interactive page model** — forms extracted (drop the `form`/`input`/`button` skip), element registry (stable ordinals), bounded `snapshots` store (TTL + GC), `web_dom` dump | fetch a form page → reader view shows annotated form blocks; `web_dom` returns the registry; snapshot resolvable after the fetch | ✓ — `extract` carries the interactive surface: document-wide `Form`/`FormField` extraction (1-based ordinals; hidden fields verbatim; labels via `aria-label`/`<label for>`/placeholder; select options + selected default), one-line annotated blocks inline + a trailer for out-of-container forms (a header search box counts); `pages.forms` column (guarded `ALTER` migration for pre-12 DBs, tested) + `snapshots` table (raw HTML, TTL working memory, `OCCIPITAL_SNAPSHOT_TTL_SECS` default 3600, pruned in `gc`, cascaded in delete, counted in stats); `Engine::dom` read-through → `web_dom` MCP tool + CLI `occipital dom` + API `GET /dom`; `web_fetch` payloads now carry `forms`. Verified LIVE (DDG `/html/`: `[form#1 → POST /html/ — text "q" · submit "Submit"]` in the reader view; registry + held snapshot via CLI, second fetch cached). 104 tests (was 88), clippy `-D warnings` clean both tiers |
| 13 | **Interaction verbs** — `web_click` (link → polite GET; submit button → form submission) + `web_submit` (fill + GET/POST), POST deliberate-only & never auto-retried, follow-along `click`/`submit` kinds | live search-box round trip on a real site via `web_submit`; click-by-ordinal navigates; POST rides the same rate/robots gates | ✓ — `Engine::click` (`link:N` → read-through GET of the href; `form:N` → submit with current values) + `Engine::submit` (override > current value > empty, in field order; hidden state verbatim; unknown field name = honest error listing the real fields; overridden radio-group names sent once; `password` values redacted in the `sent` report). GET submissions replace the action query per spec and ride the read-through cache (repeat ask = zero live requests); POST goes live once via robots-gated `HttpRequest::post_form`, result **never cached**. Fetcher hardened: `send_core` retries **GET only** — a 429/503/transport error on POST returns honestly (applies to Tavily too). Surfaces: `web_click`/`web_submit` MCP tools (9 total, kinds `click`/`submit`), CLI `occipital click`/`submit --form N --field k=v`, API `POST /click`/`/submit`. Verified LIVE: example.com `link:1` → IANA page; DDG `/html/` form#1 POST `q=…` → ranked results page (status 200) whose own registry chains (form#2 = refinement box). 115 tests (was 104), clippy `-D warnings` clean both tiers |
| 14 | **SPA salvage + honest JS signaling** — thin-body detection → mine `__NEXT_DATA__`/`__NUXT__`/state blobs/`ld+json`/OpenGraph/`noscript`; `js_required: true` when even salvage fails | fixture Next.js empty-shell page yields readable markdown (`salvaged: true`); a truly client-only page returns `js_required: true`, not silence | ✓ — new `salvage` module: `ld+json` entities (headline/description/articleBody, `@graph`-aware, HTML-in-JSON stripped), framework state blobs (`__NEXT_DATA__`, JSON-typed `__NUXT_DATA__`-style scripts, `window.__STATE__ =` assignments whose RHS is strict JSON — JS literals skipped, **nothing is ever executed**) mined via bounded prose harvesting (content-key hits + looks-like-prose filter, deduped, 30-string/6k-char cap), `noscript` fallbacks (markup stripped, "enable JavaScript" boilerplate dropped), meta/og description lead, RSS/Atom feed discovery into `links`. Trigger: content < 200 chars (form annotations excluded); `js_required` only when scripts-heavy AND no forms AND < 5 links (a lean login page is interactive, not a shell) — both outcomes annotated in the markdown AND flagged on `Page`, persisted (`pages.salvaged`/`js_required` columns via the migrate loop), surfaced across all four page payloads + `DomView` + CLI notes. Verified LIVE: bsky.app (client-only React) → `salvaged: true` with og:description + tag-stripped noscript. 127 tests (was 115), clippy `-D warnings` clean both tiers |
| 15 | **Sessions & identity** — opt-in persistent cookie jar (0600), per-domain custom headers (UA stays honest), explicit `OCCIPITAL_PROXY`; operator-provisioned auth only | a site-set cookie survives restart (opt-in) and completes a two-step flow; headers applied per-domain; proxy logged + used | ✓ — new `session` module: hand-rolled `CookieJar` implementing reqwest's `CookieStore` (reqwest's own `Jar` can't be enumerated or serialized) with RFC-6265-shaped matching — host-only vs `Domain` scope, path-match, `Secure` withheld over http, longest-path-first ordering, `Max-Age` over `Expires`, past-expiry/`Max-Age=0` as deletion, **cross-site `Domain` refused outright**; session cookies stay in memory (browser semantics), expiring ones persist `0600`. `HeaderRules`: `*` / `.suffix` / exact-host precedence, merged so explicit per-request headers (validators, provider keys) always win, with `user-agent`/`cookie`/`host`/`content-length` **not overridable** (the honest UA is locked). Explicit `OCCIPITAL_PROXY` (+ system-proxy detection now logged instead of silent). Everything opt-in: `OCCIPITAL_COOKIES=0` default → no jar exists. CLI `occipital cookies list/clear [domain]` (values redacted), `status` shows cookies/proxy/headers, and `fetch --fresh` closes the MCP/API parity gap. Verified LIVE end-to-end on httpbin: two-step flow (cookie set → carried across the redirect), an expiring cookie surviving a process restart and being re-sent (`{"cookies":{"occ_login":"tok987"}}`), `OCCIPITAL_COOKIES=0` sending nothing, per-domain + wildcard headers applied while a `User-Agent` override was refused. 136 tests (was 127), clippy `-D warnings` clean both tiers |
| 16 | **Politeness hygiene for multi-step** — honor `Crawl-delay`, eTLD+1 bucket keys, robots cache TTL, request log + trace payload, `--obey-robots` CLI alias | crawl-delay raises spacing (unit-tested); subdomain hop shares the site budget; `occipital log` shows the trail | ✓ — **`Crawl-delay` honored at last** (promised in politeness.md since Phase 1): `DomainLimiter::set_min_interval` ratchets a bucket's interval **up only**, and a slot already reserved at the old pace is pushed out (else the first post-robots request lands early — caught live); clamped at 60s with a warning so an absurd value can't hang the agent. Rate buckets keyed by **registrable domain** via a curated multi-label suffix list (deliberately not the full PSL — every error widens a bucket, i.e. errs *toward* politeness; shared-hosting suffixes listed so tenants don't queue behind each other). Robots cache keyed by **origin** and TTL'd (`OCCIPITAL_ROBOTS_TTL_SECS`, default 3600). Bounded request log (`requests` table, `OCCIPITAL_LOG_MAX` default 500, `0` disables) fed through a `RequestSink` seam so `fetch` never learns about SQLite — records method/URL/status/politeness-wait/duration **and refusals**; surfaced by CLI `occipital log` + API `GET /log` + `stats`. CLI `--obey-robots[=BOOL]` global flag (default on; off logs a loud warning). **Fixed a real politeness hole found by the live gate**: `host_str()` drops the port, so any origin not on 80/443 had its robots.txt 404 into allow-all — `Disallow` and `Crawl-delay` were silently ignored there since Phase 1. Verified LIVE against a local server publishing `Crawl-delay: 5` + `Disallow: /private/`: refusal logged and errored, allowed pages waited 5598ms (vs the 2s default pace), `--obey-robots=false` bypasses. 141 tests (was 136), clippy `-D warnings` clean both tiers |

The **JS door** (render sidecar driving a system headless browser over CDP-as-client) is a
contract, not a phase — see agent-browsing.md. It only exists as the `PageLoader` seam +
`js_required` escalation trigger until a separate `occipital-render` project claims it.

**Gate to move on:** the row's gate works end-to-end, clippy-clean, tested.

## Sequencing notes

- **Phases 0–3** are the minimum useful standalone: APEX (or any MCP client) can search + read
  the web politely. Ship this first — it's the headline capability.
- **Phases 4–6** turn it into "Cerebro for the web" — the cache, recall, and forgetting. This is
  what makes repeated/long-running research cheap and keeps results from rotting.
- **Phase 7** is opt-in quality for keyed nodes; deliberately after the scrape default works.
- **Phase 8** rounds out the standalone management story (parity with Cerebro's CLI/API).
- **Phase 9** is the only ApexOS-RS work and is purely additive (no agentd changes — see
  [follow-along.md](follow-along.md)).

## Build-time risks to watch (resolve as encountered)

- **Reader-mode crate choice.** Survey pure-Rust readability options (`readability`,
  `dom_smoothie`, `article_scraper`, or `scraper` + a hand-rolled boilerplate heuristic). Pick on
  output quality + maintenance + Nano weight. Keep it behind the `extract` module so it's swappable.
- **DuckDuckGo / SearXNG HTML drift.** Scrape parsers break when markup changes. Keep each
  provider's parser small + unit-tested against a saved fixture; fail soft (empty results + log),
  never panic.
- **Embedder reuse.** If Cerebro's embedder is extractable as a crate, depend on it (one model,
  one loader). Until then Occipital carries its own copy of the bge-small loader — keep the
  interface identical so a later swap is mechanical.
- **DB-share temptation.** Resist co-mingling with Cerebro's DB. Same model, **separate store** —
  web cache and episodic memory are different lifecycles (perishable vs earned).
- **Politeness vs. usefulness tension.** Conservative defaults may feel slow on a fast node;
  every limit is env-tunable, but the *defaults* stay polite. Document, don't loosen silently.

## Out of scope (for now)

- JS *execution* stays out of the core permanently; client-only pages get SPA salvage + an
  honest `js_required` flag (Phase 13), and heavy nodes may later opt into a render sidecar —
  see the JS-door contract in [agent-browsing.md](agent-browsing.md).
- Authenticated content beyond operator-provisioned sessions (Phase 14); paywall/CAPTCHA
  bypass stays out permanently.
- Image/PDF extraction (a later "Occipital vision" slice could mirror Cerebro's `describe_image`).
- Crawling / link-following beyond the explicit `top_n` search fetch.
